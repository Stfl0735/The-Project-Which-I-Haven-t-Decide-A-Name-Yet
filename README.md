# The-Project-Which-I-Haven-t-Decide-A-Name-Yet
A python port scanner which supposed to implement the these scanning modes in nmap:

TCP Connect Scan(Done but not implemented as a whole)

TCP SYN Scan(WIP)

TCP ACK Scan(WIP)

TCP FIN Scan(WIP)

TCP X-MAS Scan(WIP)

TCP Maimon Scan(WIP)
